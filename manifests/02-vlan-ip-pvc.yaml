apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vlan-ip-pvc
  namespace: kube-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: linode-block-storage
